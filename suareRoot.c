
#include <stdio.h>
#include <math.h>

int main(void) {
    float number;
    float squareRoot;
    printf("Enter the number: ");
    scanf("%f", &number);
    squareRoot = sqrt(number);
    printf("the square root is %f", squareRoot);
}
